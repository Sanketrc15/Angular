import { BookCurrencyPipe } from './book-currency.pipe';

describe('BookCurrencyPipe', () => {
  it('create an instance', () => {
    const pipe = new BookCurrencyPipe();
    expect(pipe).toBeTruthy();
  });
});
